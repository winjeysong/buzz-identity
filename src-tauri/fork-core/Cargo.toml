[package]
name = "fork-core"
version = "0.1.0"
edition = "2021"
description = "Knowledge snapshot, fork storage and container orchestration"

[dependencies]
globset = "0.4"
keyring = { version = "3", features = ["apple-native", "windows-native"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
